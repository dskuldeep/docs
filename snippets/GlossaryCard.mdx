export const GlossaryCard = ({ term, definition, category, link }) => {
  const termText = term || '';
  const defText = typeof definition === 'string' ? definition : (definition?.props?.children || '');
  const catText = category || '';
  
  return (
    <div style={{
      border: '1px solid #e5e7eb',
      borderRadius: '0.5rem',
      padding: '1.5rem',
      marginBottom: '1rem',
      backgroundColor: '#ffffff',
      boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.1)',
    }}>
      <div style={{
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'flex-start',
        marginBottom: '0.75rem',
      }}>
        <h3 style={{
          margin: '0',
          fontSize: '1.25rem',
          fontWeight: '600',
          color: '#0c3b43',
        }}>
          {termText}
        </h3>
        {catText && (
          <span style={{
            fontSize: '0.75rem',
            fontWeight: '500',
            color: '#6b7280',
            backgroundColor: '#f3f4f6',
            padding: '0.25rem 0.75rem',
            borderRadius: '0.375rem',
            textTransform: 'uppercase',
            letterSpacing: '0.05em',
          }}>
            {catText}
          </span>
        )}
      </div>
      <div style={{
        color: '#4b5563',
        lineHeight: '1.6',
        fontSize: '0.9375rem',
      }}>
        {definition}
      </div>
      {link && link.href && (
        <div style={{ marginTop: '0.75rem' }}>
          <a 
            href={link.href} 
            style={{
              color: '#0c3b43',
              textDecoration: 'none',
              fontSize: '0.875rem',
              fontWeight: '500',
            }}
          >
            {link.text || 'Learn more'} →
          </a>
        </div>
      )}
    </div>
  )
}
